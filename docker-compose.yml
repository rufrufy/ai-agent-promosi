version: "3.8"

services:
  ai-agent-promosi:
    build: .
    container_name: ai-agent-promosi
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
